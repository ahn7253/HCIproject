var config = {

    dbconfig: {
        connectionLimit: 10,
        host: "localhost",
        user: "root",
        password: "root",
        database: 'hci'
    },
    server:{
        host:"localhost",
        port:'3000'
    }
}

module.exports = config